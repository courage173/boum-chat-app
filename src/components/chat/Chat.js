import React from 'react';
import ChatDashboard from '../../HOC/ChatDashboard';

const Chat = () => {
    return (
        <ChatDashboard>
            <div>
                <h1>Chat page</h1>
            </div>
        </ChatDashboard>
    );
};

export default Chat;
